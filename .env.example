# Environment Configuration Example
# Copy this file to .env.local and fill in your values

# MongoDB Atlas Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?appName=mgr4smb
MONGODB_DATABASE=conversation_agent
MONGODB_MAX_POOL_SIZE=50
MONGODB_MIN_POOL_SIZE=10
# MongoDB Atlas Vector Search index name (must match index created in Atlas UI)
# See docs/ATLAS_VECTOR_SEARCH_SETUP.md for index creation instructions
VECTOR_INDEX_NAME=vector_search_index_

# Google Gemini API Configuration
GOOGLE_API_KEY=your-google-api-key-here

# Embedding Configuration
# Model: gemini-embedding-001 (3072 dimensions, free tier available)
# Pricing: Free tier - 1,500 requests/day
# See: https://ai.google.dev/pricing
EMBEDDING_MODEL=gemini-embedding-001
EMBEDDING_DIMENSIONS=3072
EMBEDDING_TASK_TYPE=retrieval_document  # retrieval_query, retrieval_document, semantic_similarity, classification, clustering


# Gmail API Configuration
# Setup: https://developers.google.com/gmail/api/quickstart/python
# 1. Enable Gmail API in Google Cloud Console
# 2. Create OAuth 2.0 credentials (Desktop app)
# 3. Download credentials.json
# 4. Run authentication flow to get refresh token
GMAIL_CLIENT_ID=your-client-id.apps.googleusercontent.com
GMAIL_CLIENT_SECRET=your-client-secret
GMAIL_REFRESH_TOKEN=your-refresh-token
GMAIL_USER_EMAIL=your-email@example.com

# Email Processing Configuration
EMAIL_POLLING_INTERVAL=300  # Poll every 5 minutes (in seconds)
EMAIL_MAX_RESULTS=100       # Maximum emails to fetch per poll
EMAIL_LABEL_FILTER=INBOX    # Gmail label to filter (INBOX, SENT, etc.)

# Environment
ENV=development  # development, staging, production

# Logging
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL

# API Security - JWT Configuration
# Generate a secure secret key (min 32 characters):
# python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-secret-key-change-in-production-min-32-chars-here
JWT_ALGORITHM=HS256

# API Clients for External Applications
# Format: client_id1=secret1,client_id2=secret2
# Example: myapp=supersecret123,webapp=anothersecret456
# Generate secure secrets: python -c "import secrets; print(secrets.token_urlsafe(24))"
API_CLIENTS=

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# API Server Configuration
API_HOST=0.0.0.0
API_PORT=8000
